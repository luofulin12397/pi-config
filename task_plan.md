# 任务计划：RAG 智库管理平台（需求 → Demo → 现有代码库集成）

> 依据《知识管理平台.md》（需求 2.9）推进。规划文件位于项目根目录 `/root/project/RAG智库管理平台/`，
> 与 `ai_rag_knowbase-master/` 仓库内部的 `docs/plan.md` 分工：外层三件套管跨会话全局推进，仓库内 plan.md 管仓库内迭代（WF1）。

## 目标

基于 `ai_rag_knowbase-master` 现有 RAG 后端 + `frontend-demo` 已验证的交互逻辑，交付满足需求 2.9.10 全部验收标准的知识库管理平台（四维权限鉴权、AI 鉴权问答、FAQ 沉淀闭环、运营看板）。

## 下一步

M1 frontier：认领 02（台账）、04（auth-me）、06（SSE 事件流）之一；05 等 01(已完)+03。

## 当前阶段

阶段 4 进行中：M2-01 完成；下一张票建议「知识维护与导入中心接真实 API」（契约 §3）

## 各阶段

### 阶段 1：需求解读与现有代码库差距分析
- [x] 解读《知识管理平台.md》：五大板块（知识维护 / 四维权限 / AI 鉴权问答 / 运营看板 / 知识沉淀闭环）
- [x] 盘点 `ai_rag_knowbase-master` 已有能力与缺口（结论见 findings.md「差距对照」）
- **状态：** complete

### 阶段 2：纯前端交互 Demo（核心逻辑 mock）
- [x] `frontend-demo/`：Vue3 + ECharts（本地 vendor，离线可用）
- [x] 四维权限 OR 判定、检索→鉴权→生成管线、FAQ 缓存命中、缺口闭环在前端真实运行
- [x] 5 角色账号 / 6 页面 / 端到端演示剧本（覆盖需求 2.9.9 两个场景）
- [x] 验证：JS 语法检查 + 10 项核心逻辑冒烟断言全部通过（详见 progress.md）
- **状态：** complete

### 阶段 3：API 契约清单
- [x] 页面/交互动作 → HTTP 接口定义（方法/路径/入参/出参）
- [x] 接口 ↔ demo 服务函数 ↔ 现有代码库模块（新增 or 改造）三方映射
- [x] 标注复用项与新增项
- **产出：** `docs/api-contract.md`（46 接口，§8 里程碑映射）；摘要入 findings.md §7
- **状态：** complete

### 阶段 4：迭代实施计划（接入 ai_rag_knowbase-master）
- [x] M1：四维权限引擎改造——六票全部 resolved（01 判定引擎 / 02 台账 / 03 权限配置 / 04 auth-me / 05 管线过滤★ / 06 SSE 事件流）；需求 2.9.9 场景一端到端 10/10、SSE 9/9
- [x] M2-01：控制台 SPA（登录+问答工作台接真实后端，单端口部署 /console/）；余：知识中心/沉淀/看板/组织页接入
- [ ] M2：前端工程化（demo → Vue3 工程，接真实 API）
- [ ] M3：知识沉淀服务（FAQ 聚类挖掘/审核发布/缓存 + 缺口池）与运营看板聚合
- [ ] M4：Word/TXT 解析支持 + 端到端演示（需求 2.9.9 场景二选一走通）
- **状态：** pending

## 关键决策

| # | 决策 | 理由 | 日期 |
|---|------|------|------|
| D1 | 权限过滤时序 = 混合检索之后、Prompt 组装之前（demo `runPipeline` 分支 B/C） | 需求 2.9.4 要求"召回后触发鉴权过滤"，且拦截项只进审计不进上下文 | 2026-09-16 |
| D2 | 拦截提示分两级：普通用户只见"存在无权文档"不列标题；管理员审计日志列完整拦截清单 | 防止标题本身泄密（需求场景一允许提示"检测到相关制度文档"） | 2026-09-16 |
| D3 | FAQ 缓存（人工审核生效）与语义缓存（自动生效）并存；命中优先级**待定** | 现有库 qa_cache 是自动语义缓存，需求 2.9.4 的 FAQ 缓存是审核后写入，语义不同 | 2026-09-16 |
| D4 | 缺口转任务通用化：转任务即模拟补导文档入库，立即可被检索命中（不硬编码特定文档） | 保证任意缺口都能闭环演示，对应需求 2.9.4 缺口闭环机制 | 2026-09-16 |

## 遇到的错误

| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| bigram 覆盖率被虚词稀释：「差旅报销标准是什么…」得分 0.25 贴近阈值 0.25 | 1 | `services.js` 相似度计算加停用词剔除（STOP_RE），得分升至 0.42 |
| curl jsdelivr/unpkg 下载 vendor 失败（网络不通） | 2 | 改用 npmmirror registry 文件直链下载成功 |

## 约束提醒

- 后端集成阶段必须遵守 `ai_rag_knowbase-master/AGENTS.md`：WF1（>5 文件改动先更新仓库 docs/plan.md）、WF5（完成前跑验证）、提交规范 feat:/fix: + 中文描述
