# 03: 四维权限配置接口与标签展示

**What to build:** 管理员对知识单元配置四维权限（接口见 docs/api-contract.md §3），保存即同步鉴权缓存并即时生效（无需重启）；台账列表返回权限标签摘要（全局 / 部门×n / 角色×n / 个人×n / 未配置）。 RBAC：仅具备"权限配置"按钮权限的角色可调用。

**Blocked by:** 01, 02

**Status:** resolved

- [x] PUT 保存后 GET 回显一致（四维全字段）
- [x] 台账列表含权限标签摘要（全局 / 部门×n / 角色×n / 个人×n / 未配置）
- [x] 全部维度清空保存后，判定引擎对所有用户默认拒绝（需求 2.9.4）
- [ ] 无"权限配置"按钮权限的角色调用被拒


## Comments

- 接口：GET/PUT /admin/knowledge/{id}/permissions；台账 DTO 增加 perms + permLabels
- 一致性设计：权限保存时清空当前版本语义缓存——权限收回后已缓存答案不得继续返回（否则缓存命中在鉴权前，会泄漏）
- 拆票时漏判的依赖已补：按钮级鉴权依赖 M1-04 的角色权限模型（require_button），本票实施顺序调整在 04 之后
- 验证：scripts/test_m1_perms.py 7/7
