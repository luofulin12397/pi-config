# docker compose 插值变量（compose 会自动读取本目录下的 .env）
#
#   cp deploy/.env.example deploy/.env && vi deploy/.env
#
# 注意：这里不是容器内环境变量（那些在仓库根的 .env，由 compose 的 env_file 注入）；
#      这里只放 compose 文件里 ${...} 插值用的变量。
#
# PUBLIC_HOST：服务器 IP 或域名。用于拼接「浏览器可访问」的 MinIO 图片地址。
#   不改的话会回退 host.docker.internal，只适用于浏览器与服务器同机的场景。
#   例：PUBLIC_HOST=192.168.1.10 或 rag.example.com
PUBLIC_HOST=host.docker.internal
